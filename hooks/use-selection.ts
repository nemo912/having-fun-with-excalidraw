"use client";

import { DrawnElementType, XYWH } from "@/types";
import { selector, selectorFamily, useRecoilCallback, useRecoilValue } from "recoil";

// export const useSelectionBounds = (drawnElement: DrawnElementType) => {
//     return boundingBox(drawnElement);
// };
